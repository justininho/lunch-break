extends Node2D

#func _ready():
	# Connect to all entity examination signals
	#get_tree().connect("entity_examined", self, "_on_entity_examined")
	#get_tree().connect("interacted", _on_interacted)	
	#pass
#
#func _on_interacted(entity: Interactable) -> void:
	#print('player interacted with: ', entity)
#func _on_entity_examined(entity_data):
	#update_dialog_content(entity_data)
	#show()
		
# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
